---
trigger: glob
globs: packages/*/docs/*/component_*.md
---

# 컴포넌트별 README 문서작성

## Rules

1. 파일이 추가된 폴더명을 참고해서 해당 언어에 따라 문서를 작성한다.
2. 컴포넌트의 이름은 작성하는 파일명과 동일하다.(파일은 사용자가 직접 추가할 것이다.)
3. 한글문서를 먼저 작성하고 작성된 한글문서를 바탕으로 영문문서를 작성한다.
4. 문서 상단의 링크는 다른 언어로 작성된 컴포넌트 문서로 이동하기 위한 링크이며 상대경로로 작성한다.
5. 한글문서의 템플릿은 아래와 같다.

   ````md
   # {컴포넌트명}

   [English]({상대경로}) | 한국어

   // 컴포넌트에 대한 간단 설명을 5줄 내외로 작성한다.
   // ex)
   // - 커스텀 ContextMenu 컴포넌트를 개발할 때 사용하는 컴포넌트입니다.
   // - 커스텀 ContextMenu를 개발한다고 해도, 이를 위해 필요한 요소들이 있어 반드시 이 컴포넌트를 연결해서 사용하셔야 합니다.
   // - 만약 이를 사용하지 않으면 정상적인 기능동작이 어렵습니다.

   ## 기본 사용 예시 // 영문 버전에선 "Example"

   ```tsx
   // 블록만 추가하고 내용은 작성하지 말것
   // 내용은 사용자가 직접 작성할 예정
   ```

   ## API(Props) 설명 // 영문 버전에선 "API(Props) Reference"

   // 컴포넌트 개수에 따라 분리해서 추가할것

   ### {컴포넌트명}

   | Name      | Type                          | Required         | Default                     | Description |
   | :-------- | :---------------------------- | :--------------- | :-------------------------- | :---------- |
   | {props명} | {props 타입(TypeScript 기준)} | {필수 선언 여부} | {기본값(없으면 "-"로 선언)} | {설명}      |

   ...

   // 만약 자체 선언한 타입이 내부적으로 존재하는 경우 아래와 같이 Types 영역을 추가할것
   // 없으면 추가하지 않아도 됨.

   ### Types

   #### {타입명}

   | Name      | Type                          | Required         | Default                     | Description |
   | :-------- | :---------------------------- | :--------------- | :-------------------------- | :---------- |
   | {props명} | {props 타입(TypeScript 기준)} | {필수 선언 여부} | {기본값(없으면 "-"로 선언)} | {설명}      |
   ````
